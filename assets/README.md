# assets

素材をこのフォルダに置きます。

## フォント

| ファイル名 | 用途 |
| --- | --- |
| LINESeedJP_OTF_Rg.woff2 | 通常書体（400） |
| LINESeedJP_OTF_Bd.woff2 | 太字書体（700） |

フォントがない場合は端末のフォントで表示します。

## 音声

音声ファイルのパスは`config.js`の`assets`で指定します。未指定の場合は無音です。

音量は設定画面で調整します。
